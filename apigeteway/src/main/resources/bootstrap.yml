spring:
  application:
    name: api-geteway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev

eureka:
  client:
    service-url:
      defalutZone: http://localhost:8761/eureka


